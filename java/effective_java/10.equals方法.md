# equals方法

---

## 示例
(含特性)

~~~java

/**
 * 第十章: 关于equals的相关内容
 * 结论1: 如果需要进行逻辑上的校验,那么可以覆盖equals方法
 * 如果仅仅是值上的比较,一个值对应一个类比如枚举类,这时候则没有必要去覆盖equals方法
 * 如果equals本身不是侧重点,且被调用的时候容易出现混淆的观念的时候,比如无法判断具体是要比较值还是特征的时候,就不建议开放equals方法
 */
public class Equals {

    /**
     * equals方法必须要保证的几个特性:
     * 1.自反性:
     * 类必须等于自身,很难违背,但是违背了会造成很多大问题
     */

    /**
     * 2.对称性,a和b是否相等和b和a是否相等的判断规则必须一致
     *
     * */

    /**
     * 3.传递性
     * 如果a等于b且b等于c,那么这个时候a必须等于c
     */

    /**
     * 4.一致性
     * 如果a等于b,再次判断的时候a仍然等于b
     */

    /**
     * 5.非空性
     */

    /**
     * 从上面就可以知道了,equals最后有一个统一的判定规则
     */
}

~~~
